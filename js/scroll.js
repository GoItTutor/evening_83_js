// 'use strict';

// const randomColor = () =>
//   '#' + (Math.random().toString(16) + '000000').substring(2, 8).toUpperCase();

// const sectionElements = document.querySelectorAll('.section');

// const animatedSectionEl = document.querySelector('.animated-section');

// sectionElements.forEach(section => {
//   section.style.backgroundColor = randomColor();
// });

// const handleDocumentScroll = () => {
//   console.log(window.pageYOffset);

//   if (window.pageYOffset >= 416) {
//     animatedSectionEl.classList.add('active');
//   }
// };

// const throttledFunction = _.throttle(handleDocumentScroll, 4500);

// document.addEventListener('scroll', throttledFunction);
