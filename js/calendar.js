// 'use strict';

// const calendarBodyEl = document.querySelector('tbody');

// let prevTdEl = null;

// const handleCalendarClick = event => {
//   if (event.target.textContent === '' || event.target.nodeName !== 'TD') {
//     return;
//   }

//   if (prevTdEl === event.target) {
//     event.target.classList.toggle('active');

//     return;
//   }

//   if (prevTdEl) {
//     prevTdEl.classList.remove('active');
//   }

//   event.target.classList.add('active');

//   prevTdEl = event.target;
// };

// calendarBodyEl.addEventListener('click', handleCalendarClick);
